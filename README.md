# Nesine Next App (TypeScript + Redux)

Scaffolded Next.js app (Next ^16) with TypeScript, ESLint, Prettier and Redux Toolkit.

Quick start:

```bash
npm install
npm run dev
```

Lint:

```bash
npm run lint
```

Format:

```bash
npm run format
```
